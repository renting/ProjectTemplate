# App name — used as localStorage key prefix to avoid collisions between projects
VITE_APP_NAME=VueAppAdmin

# Base path for production deployment (e.g., "/" for root, "/admin/" for sub-path)
VITE_DEFINECONFIG_BASE=/
